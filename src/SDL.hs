{-|
You should probably just import this as SDL and call SDL.Decl
-}

module SDL
  (
    module SDL.Assert,
    module SDL.AsyncIO,
    module SDL.Atomic,
    module SDL.Bits,
    module SDL.BlendMode,
    module SDL.Endian,
    module SDL.Error,
    module SDL.IOStream,
    module SDL.Init,
    module SDL.Mutex,
    module SDL.Pixels,
    module SDL.Properties,
    module SDL.Stdinc,
    module SDL.Thread,
    module SDL.Timer,
    module SDL.Version
  ) where

import SDL.Assert
import SDL.AsyncIO
import SDL.Atomic
import SDL.Bits
import SDL.BlendMode
import SDL.Endian
import SDL.Error
import SDL.IOStream
import SDL.Init
import SDL.Mutex
import SDL.Pixels
import SDL.Properties
import SDL.Stdinc
import SDL.Thread
import SDL.Timer
import SDL.Version

